# Variáveis de Ambiente
.env
.env.local
.env.*.local

# Chaves de API e Credenciais
*.key
*.pem
*.secret
*.ppk

# Lembrete local de acesso à VM (IP Tailscale, caminho de chave privada)
ACESSO-VM-TAILSCALE.txt

# Benchmark local do Image Engine (script + thumbnails baixadas do Unsplash
# pra diagnostico manual) — dado de trabalho local, nao faz parte do produto
_benchmark_image_engine/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual Environments
venv/
ENV/
env/
.venv

# Node.js
node_modules/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Logs
*.log
logs/

# Configs gerados
configs/
site-configs/
lovable_prompts/
prompt_teste_lovable.txt
*.json
!site-config-example.json
!site-config.json
!vendas-config.json
!/package.json
!/package-lock.json
!backend/data/niches.json
!backend/data/buscas_leads.json

# Métricas e dados
metrics.json
relatorio_testes.json

# Logos de clientes gerados via onboarding (dados por cliente, não versionados)
assets/logos/*
!assets/logos/.gitkeep

# Leads reais da Fase 2 (dado de terceiro, não versionado) - o .example.csv
# fica versionado como template/estrutura
leads/*.csv
!leads/*.example.csv

# Compatibilidade com Codex (não usado neste projeto atualmente)
AGENTS.md
.agents/

# Estado local de sessão do Claude Code (não é código-fonte do projeto)
.claude/projects/

# Cache de imagens curadas por categoria (Unsplash) - estado gerado em
# runtime, não código-fonte
backend/cache/
